# Dependencies cho webtoon-translator pipeline
# Cài: pip install -r requirements.txt

# Image processing
Pillow>=10.0.0
numpy>=1.24

# Config
PyYAML>=6.0

# OpenAI client (cho Ollama Cloud + glossary LLM extract)
openai>=1.0.0

# LƯU Ý: manga-image-translator (engine chính) KHÔNG có trên PyPI.
# Phải cài từ source (xem README):
#   git clone --depth 1 https://github.com/zyddnys/manga-image-translator ../manga-image-translator
#   pip install -r ../manga-image-translator/requirements.txt
#   pip install -e ../manga-image-translator
